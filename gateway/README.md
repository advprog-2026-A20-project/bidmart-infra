# Gateway Config

Direktori ini disiapkan untuk menyimpan konfigurasi gateway (misalnya Nginx/Traefik/Envoy) saat aturan routing sudah dipisah dari kode aplikasi.

## TODO
- Tambahkan file config runtime gateway.
- Sinkronkan route prefix dengan `docs/routing-table.md`.
